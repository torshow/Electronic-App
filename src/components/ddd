:root {
  font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  color: #2c2925;
  background: #f7f3ed;
  font-synthesis: none;
  text-rendering: optimizeLegibility;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-width: 320px;
}

button,
input,
select,
textarea {
  font: inherit;
}

.site-header {
  background: #315a45;
  color: white;
  padding: 2rem 1rem;
}

.header-content,
.page-container {
  width: min(1100px, 92%);
  margin: 0 auto;
}

.site-header h1,
.site-header p,
h2,
h3 {
  margin-top: 0;
}

.site-header p {
  margin-bottom: 0;
  color: #dce9df;
}

.page-container {
  padding: 2.5rem 0 4rem;
}

.intro-section {
  display: grid;
  grid-template-columns: 1.4fr 1fr;
  gap: 2rem;
  align-items: end;
  margin-bottom: 2rem;
}

.intro-section h2 {
  font-size: clamp(2rem, 5vw, 3.5rem);
  max-width: 650px;
  margin-bottom: 0.75rem;
}

.intro-section p {
  max-width: 650px;
  line-height: 1.7;
}

.eyebrow {
  color: #b05d38;
  font-size: 0.8rem;
  font-weight: 800;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.search-box,
.recipe-form label {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  font-weight: 700;
}

input,
select,
textarea {
  width: 100%;
  border: 1px solid #d6cec3;
  border-radius: 10px;
  background: white;
  padding: 0.85rem 1rem;
  color: #2c2925;
}

input:focus,
select:focus,
textarea:focus {
  outline: 3px solid rgba(49, 90, 69, 0.15);
  border-color: #315a45;
}

.form-section {
  background: #fffdf9;
  border: 1px solid #e4ddd3;
  border-radius: 18px;
  padding: 1.5rem;
  margin-bottom: 3rem;
  box-shadow: 0 12px 35px rgba(72, 57, 42, 0.08);
}

.section-heading {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 1rem;
  margin-bottom: 1.25rem;
}

.section-heading h2,
.section-heading p {
  margin-bottom: 0;
}

.section-heading span {
  color: #6d665f;
}

.dispenser-form {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 1rem;
}

.full-width {
  grid-column: 1 / -1;
}

.dispenser-form button {
  justify-self: start;
  border: 0;
  border-radius: 10px;
  background: #b05d38;
  color: white;
  padding: 0.85rem 1.4rem;
  font-weight: 800;
  cursor: pointer;
}

.dispenser-form button:hover {
  background: #91482b;
}

.dispenser-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 1.25rem;
}

.dispenser-card {
  overflow: hidden;
  border-radius: 16px;
  background: white;
  box-shadow: 0 10px 30px rgba(72, 57, 42, 0.1);
}

.dispenser-card img {
  width: 100%;
  height: 210px;
  object-fit: cover;
  display: block;
}

.dispenser-card-content {
  padding: 1.25rem;
}

.card-meta {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  color: #6f786f;
  font-size: 0.85rem;
  font-weight: 700;
}

.dispenser-card h3 {
  margin: 0.8rem 0 0.5rem;
}

.recipe-card p {
  margin: 0;
  color: #67615b;
  line-height: 1.6;
}

.empty-message {
  border: 1px dashed #c6bbad;
  border-radius: 12px;
  padding: 2rem;
  text-align: center;
}

@media (max-width: 800px) {
  .intro-section,
  .recipe-grid {
    grid-template-columns: 1fr 1fr;
  }

  .intro-section > div {
    grid-column: 1 / -1;
  }
}

@media (max-width: 600px) {
  .intro-section,
  .recipe-form,
  .recipe-grid {
    grid-template-columns: 1fr;
  }

  .full-width {
    grid-column: auto;
  }

  .section-heading {
    align-items: flex-start;
    flex-direction: column;
  }
}
